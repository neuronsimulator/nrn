#!/usr/bin/env bash
if [[ -n "@NRN_SANITIZER_LIBRARY_PATH@" ]]; then
  LD_PRELOAD=@NRN_SANITIZER_LIBRARY_PATH@ @NRN_SANITIZER_ENABLE_ENVIRONMENT@ "$@"
else
  @NRN_SANITIZER_ENABLE_ENVIRONMENT@ "$@"
fi

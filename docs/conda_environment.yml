name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - bison
  - cmake
  - xorg-libxcomposite
  - ffmpeg
  - cython
  - pandoc
  - pip
  - pip:
      - -r docs_requirements.txt
